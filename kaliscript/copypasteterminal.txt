passwd && sudo apt update && sudo DEBIAN_FRONTEND=noninteractive NEEDRESTART_MODE=a apt upgrade -y && wget https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb -P ~/ && sudo DEBIAN_FRONTEND=noninteractive NEEDRESTART_MODE=a apt install gobuster tealdeer ~/google-chrome-stable_current_amd64.deb -y && tldr -u && rm -rfv ~/google-chrome-stable_current_amd64.deb && mkdir ~/ctf && sudo reboot now
